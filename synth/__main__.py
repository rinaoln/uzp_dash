"""Сборка синтетической БД открытого контура: `python3 -m synth`.

Пересоздаёт схему, грузит CSV-справочники (data/), генерирует синтетику.
Только для открытого контура — на закрытом используется пром-БД.
"""
from . import build

if __name__ == "__main__":
    counts = build()
    print("Синтетика собрана:")
    for table, n in counts.items():
        print(f"  {table:35} {n:>8}")
